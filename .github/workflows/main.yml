name: Deploy to Server
'on':
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploy Code to Server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Sync files to server
        run: echo "helloooo"
        env:
          SSH_PRIVATE_KEY: '${{ secrets.TEST_PRIVATE_KEY }}'
